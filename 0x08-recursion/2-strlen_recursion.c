#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - function that returns length of string
 * @s: parameter string to be considered
 *
 * Return: return i
 */

int _strlen_recursion(char *s)
{
	/*declairing an integer*/
	int i;

	i = strlen(s); /*assigning string length to i*/
	return (i);
}
