#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - function that prints a string then newline
 * @s: paramter pointer to the string
 *
 * Return: return void
 */

void _puts_recursion(char *s)
{
	puts(s);
}
