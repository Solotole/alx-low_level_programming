#include <stdio.h>

int main(int argc, char *argv[])
{
	int a, b, c, d;

	a = atoi(argv[1]);
	b = atoi(argv[2]);
	c = atoi(argv[3]);
	if (argc ! 4)
	{
		printf("Error\n");
		exit(98);
	}
	if (b != + || b != - || b != * || b != / || b != %)
	{
		printf("Error\n");
		exit(99);
	}
	if ((b = / || b = %) && c = 0)
	{
		printf("Error\n");
		exit(100);
	}
}
