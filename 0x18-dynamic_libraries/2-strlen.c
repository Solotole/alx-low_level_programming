#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: parameter pointer
 *
 * Return: always return (num)
 */

int _strlen(char *s)
{
	int num;

	num = strlen(s);
	return (num);
}
