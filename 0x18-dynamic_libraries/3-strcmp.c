#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares 2 strings
 * @s1: first pointer string
 * @s2: second pointer string
 *
 * Return: return i
 */

int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
