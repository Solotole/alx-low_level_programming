#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts - function that prints a string then a new line
 * @str: pointer parameter
 *
 * Return: return void
 */

void _puts(char *str)
{
	puts(str);
}
