#include <stdio.h>

/**
 * main - main entry point of program
 *
 * Return: always return 0
 */
int main(void)
{
	int i = 0;

	while (i <= 9)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
